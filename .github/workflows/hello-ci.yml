name: Hello GitHub Actions
on:
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "hello from subhan"
      - name: Hello with name
        run: echo "Hello Subhan"        
      - name: Show date
        run: date
      - name: Show operating system
        run: uname -a
